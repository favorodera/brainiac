{
  "name": "nuxt-app",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "format": "prettier --write ./",
    "deploy": "bun run format && bun run build -- --preset=firebase && firebase deploy hosting && git add ."
  },
  "dependencies": {
    "@google/generative-ai": "^0.16.0",
    "@nuxt/ui": "^2.18.0",
    "@unocss/reset": "^0.61.5",
    "firebase": "^10.12.4",
    "nuxt": "^3.12.4",
    "unocss": "^0.61.5",
    "vue": "latest"
  },
  "devDependencies": {
    "@iconify/json": "^2.2.230",
    "@unocss/nuxt": "^0.61.5",
    "prettier": "3.3.3",
    "sass": "^1.77.8"
  }
}
